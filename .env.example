# Deal Desk — local settings. Do not commit (already in .gitignore).
ADMIN_USER=admin
ADMIN_PASSWORD_HASH=<scrypt hash — generate with: python3 tools/mkpass.py>
SESSION_SECRET=<random 48 bytes>
INGEST_TOKEN=<bearer token for ChatGPT tasks + Discord relay>
WORKER_TOKEN=<bearer token for the checkout worker>
DB_PATH=data/dealbot.db
ALLOW_NETWORK=1
AUTO_BUY=0
MAX_SPEND_PER_DEAL=1500
MAX_SPEND_PER_DAY=5000
